# NPM Configuration
# Ensures consistent package installation across all environments

# Use npm v8+ flat dependency tree (default in npm 7+)
legacy-peer-deps=false

# Strict SSL verification
strict-ssl=true

# Save exact versions (no ^ or ~)
save-exact=false

# Use npm audit for security scanning
audit=true
audit-level=moderate

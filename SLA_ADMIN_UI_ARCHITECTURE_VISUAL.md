# SLA Admin UI - Visual Reference & Architecture

**Date:** February 25, 2026  
**Status:** ✅ Production Ready

---

## 📍 Navigation Map

```
┌─────────────────────────────────────────────────────────────────┐
│                         LOGIN PAGE                              │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ├─ [admin account]
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ADMIN DASHBOARD                              │
│  ┌───────────────────────┐  ┌──────────────────────────────┐   │
│  │   SIDEBAR MENU        │  │   MAIN CONTENT AREA          │   │
│  ├───────────────────────┤  │                              │   │
│  │ • Dashboard           │  │  Stats Cards, Activity Log   │   │
│  │ • Users               │  │                              │   │
│  │ • Public Pages        │  │                              │   │
│  │ • All Records         │  │                              │   │
│  │ • Legacy Records      │  │                              │   │
│  │ • Deleted Archive     │  │                              │   │
│  │ • Assignments         │  │                              │   │
│  │ • Departments         │  │                              │   │
│  │ • SLA Policies    ◄── ├─→│ [CLICK TO OPEN SLA UI]      │   │
│  │ • Analytics           │  │                              │   │
│  │ • Settings            │  │                              │   │
│  └───────────────────────┘  └──────────────────────────────┘   │
└────────────────┬─────────────────────────────────────────────────┘
                 │
                 │ [Click "SLA Policies"]
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│         SLA POLICIES MANAGEMENT PAGE                            │
│  /dashboard/sla-policies                                        │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ SLA Policy Management                                     │  │
│  │ Configure Service Level Agreement durations              │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ [SUCCESS MESSAGE: SLA updated successfully!]              │  │
│  │ (appears for 3 seconds after save)                        │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ Supervisor Review                          [Active]       │  │
│  │ Duration Days: [7    ]  Grace Days: [2    ]              │  │
│  │ Allow Extensions: [✓] Max: [2  ] Days: [7  ]             │  │
│  │                              [SAVE CHANGES]              │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ Evaluation                                 [Active]       │  │
│  │ Duration Days: [10   ]  Grace Days: [2    ]              │  │
│  │ Allow Extensions: [✓] Max: [2  ] Days: [7  ]             │  │
│  │                              [SAVE CHANGES]              │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ Revision Requested                         [Active]       │  │
│  │ Duration Days: [14   ]  Grace Days: [3    ]              │  │
│  │ Allow Extensions: [✓] Max: [3  ] Days: [7  ]             │  │
│  │                              [SAVE CHANGES]              │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ Materials Requested                        [Active]       │  │
│  │ Duration Days: [7    ]  Grace Days: [2    ]              │  │
│  │ Allow Extensions: [✓] Max: [2  ] Days: [7  ]             │  │
│  │                              [SAVE CHANGES]              │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ Certificate Issued                         [Active]       │  │
│  │ Duration Days: [3    ]  Grace Days: [0    ]              │  │
│  │ Allow Extensions: [ ]  (disabled when unchecked)         │  │
│  │                              [SAVE CHANGES]              │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ Understanding SLA Policies                                │  │
│  │ Duration: Standard time to complete                      │  │
│  │ Grace Period: Buffer before OVERDUE                      │  │
│  │ Extensions: Allow deadline extensions if enabled         │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🏗️ System Architecture

### Frontend Layer
```
App.tsx
├── AuthContext (checks profile.role)
├── DashboardLayout
│   ├── Navigation Sidebar
│   │   └── filteredNavItems (only admin sees SLA Policies)
│   └── Route: /dashboard/sla-policies
│       └── AdminSLAManagement.tsx
│           ├── Check: profile?.role === 'admin'
│           │   ├── true  → render UI
│           │   └── false → render "Access Denied"
│           ├── Fetch: SELECT * FROM workflow_sla_policies
│           ├── Display: 5 stage cards with settings
│           ├── Edit: inline form fields
│           └── Save: UPDATE workflow_sla_policies
```

### Database Layer
```
workflow_sla_policies (RLS Protected)
├── SELECT: Authenticated users (read-only)
│   └── Can read policies for deadline calculations
├── INSERT: Admins only
├── UPDATE: Admins only
│   └── Updates applied to future stages
└── DELETE: Admins only

workflow_stage_instances
├── Links to workflow_sla_policies for deadline calc
├── Stores due_at, extended_until, status
└── NOT modified by admin UI (reference only)
```

### Data Flow
```
1. Admin opens /dashboard/sla-policies
   ↓
2. AdminSLAManagement.tsx fetches policies
   ├── SELECT * FROM workflow_sla_policies
   └── Rendered in 5 cards
   ↓
3. Admin edits a field (e.g., duration_days: 7 → 8)
   ↓
4. Admin clicks "Save Changes"
   ├── Validates: duration_days >= 1 ✓
   ├── Sends UPDATE request
   ├── Database RLS checks: is user admin? ✓
   ├── UPDATE workflow_sla_policies SET duration_days = 8
   └── Timestamp field auto-updated: updated_at = now()
   ↓
5. Response received, success message shown
   ↓
6. Next NEW workflow_stage_instances will use new duration
```

---

## 🔐 Access Control Flow

### Admin User Path
```
┌─────────────┐
│ Admin Logs  │
│    In       │
└──────┬──────┘
       │
       ▼
┌───────────────────────────────────────┐
│ DashboardLayout                       │
│ const filteredNavItems =              │
│   navItems.filter(item =>             │
│     profile.roles.includes(role)      │
│   )                                   │
└──────┬──────────────────────────────────┘
       │
       ├─ admin? YES ✓
       │
       ▼
┌───────────────────────────────────────┐
│ Navy sidebar shows "SLA Policies"     │
└──────┬───────────────────────────────┘
       │
       ▼ [Click]
┌───────────────────────────────────────┐
│ AdminSLAManagement.tsx                │
│ if (profile?.role !== 'admin') {      │
│   return <AccessDenied />             │
│ }                                     │
└──────┬───────────────────────────────┘
       │
       ├─ admin? YES ✓
       │
       ▼
┌───────────────────────────────────────┐
│ Load policies from database           │
│ SELECT * FROM workflow_sla_policies   │
│ [RLS allows read for auth users]      │
└──────┬───────────────────────────────┘
       │
       ▼
┌───────────────────────────────────────┐
│ Render SLA Management UI              │
│ User can edit all fields              │
└──────┬───────────────────────────────┘
       │
       ▼ [Save Changes]
┌───────────────────────────────────────┐
│ UPDATE workflow_sla_policies          │
│ WHERE id = policy.id                  │
│ [RLS checks: is admin? YES ✓]         │
└──────┬───────────────────────────────┘
       │
       ▼
┌───────────────────────────────────────┐
│ Success message displayed             │
│ Changes apply to NEW records          │
└───────────────────────────────────────┘
```

### Non-Admin User Path
```
┌──────────────┐
│ User Logs In │
│ (not admin)  │
└──────┬───────┘
       │
       ▼
┌───────────────────────────────────────┐
│ DashboardLayout                       │
│ const filteredNavItems =              │
│   navItems.filter(item =>             │
│     profile.roles.includes(role)      │
│   )                                   │
└──────┬───────────────────────────────┘
       │
       ├─ admin? NO ✗
       │
       ▼
┌───────────────────────────────────────┐
│ Navy sidebar DOES NOT show            │
│ "SLA Policies" menu item              │
└───────────────────────────────────────┘

[If user somehow navigates to /dashboard/sla-policies]
       │
       ▼
┌───────────────────────────────────────┐
│ AdminSLAManagement.tsx                │
│ if (profile?.role !== 'admin') {      │
│   return <AccessDenied />             │
│ }                                     │
└──────┬───────────────────────────────┘
       │
       ├─ admin? NO ✗
       │
       ▼
┌───────────────────────────────────────┐
│ ACCESS DENIED MESSAGE                 │
│ "Only administrators can manage       │
│  SLA policies."                       │
└───────────────────────────────────────┘
```

---

## 📊 State Diagram

```
                    ┌─────────────────────┐
                    │   Page Loads        │
                    │ Fetch Policies      │
                    └─────────┬───────────┘
                              │
                    ┌─────────▼───────────┐
                    │   Loading State     │
                    │   [Spinner]         │
                    └─────────┬───────────┘
                              │
        ┌─────────────────────┴──────────────────────┐
        │                                            │
        ▼                                            ▼
┌──────────────────────┐            ┌────────────────────────┐
│ Error State          │            │ Success State          │
│ [Red Error Box]      │            │ Display Policies       │
│ "Failed to load..."  │            │ Editable Fields        │
└──────────────────────┘            └──────────┬─────────────┘
                                               │
                          ┌────────────────────┘
                          │
                    ┌─────▼──────────┐
                    │ Edit Field(s)  │
                    │ [typing...]    │
                    └─────┬──────────┘
                          │
                    ┌─────▼─────────────────────┐
                    │ Click "Save Changes"      │
                    └─────┬─────────────────────┘
                          │
                          ▼
                    ┌──────────────────────┐
                    │ Saving State         │
                    │ Button: "Saving..."  │
                    │ [disabled]           │
                    └─────┬────────────────┘
                          │
        ┌─────────────────┴──────────────────┐
        │                                    │
        ▼                                    ▼
┌──────────────────────┐        ┌─────────────────────┐
│ Error on Save        │        │ Save Success        │
│ [Red Alert]          │        │ [Green Success Msg] │
│ "Failed to save..."  │        │ "SLA updated!"      │
└──────────────────────┘        │ [Auto-dismiss 3s]   │
        │                       └─────────┬───────────┘
        │                                 │
        │ [User corrects & retries]       │
        │                                 │
        └──────────────────┬──────────────┘
                           │
                    ┌──────▼────────┐
                    │ Back to Edit   │
                    │ State          │
                    └────────────────┘
```

---

## 🎯 Navigation Tree

```
/dashboard                              (requires authentication)
├─ /                                    → ApplicantDashboard (default for applicant)
│                                       → SupervisorDashboard (default for supervisor)
│                                       → EvaluatorDashboard (default for evaluator)
│                                       → AdminDashboard (default for admin)
│
├─ /submissions                         → ApplicantDashboard
├─ /submit                              → NewSubmissionPage
│
├─ /review                              → SupervisorDashboard
├─ /evaluations                         → EvaluatorDashboard
│
├─ /users                               → UserManagement (admin-only)
├─ /public-pages                        → PublicPagesManagement (admin-only)
├─ /records                             → AllRecordsPage (admin-only)
├─ /legacy-records                      → LegacyRecordsPage (admin-only)
├─ /deleted-records                     → DeletedArchivePage (admin-only)
├─ /assignments                         → AssignmentManagementPage (admin-only)
├─ /departments                         → DepartmentManagementPage (admin-only)
├─ /sla-policies                        → AdminSLAManagement (admin-only) ◄── NEW
├─ /analytics                           → AdminDashboard (admin-only)
├─ /settings                            → SettingsPage (all users)
└─ /branding                            → AdminBrandingSettingsPage (admin-only)
```

---

## 🔄 Component Hierarchy

```
App.tsx
└─ BrowserRouter
   └─ AuthProvider
      └─ ErrorBoundary
         └─ Routes
            ├─ /                        → LandingPage
            ├─ /login                   → LoginPage
            ├─ /register                → RegisterPage
            ├─ /auth/callback           → AuthCallbackPage
            └─ /dashboard/*             → ProtectedRoute
               └─ DashboardRouter
                  └─ DashboardLayout
                     ├─ Header (with NotificationCenter)
                     ├─ Sidebar (with filtered navItems)
                     │  └─ SLA Policies ◄── NEW nav item
                     └─ Routes
                        ├─ /                → getDashboardComponent()
                        ├─ /sla-policies    → AdminSLAManagement ◄── NEW
                        ├─ /settings        → SettingsPage
                        └─ ... (other routes)
```

---

## 📋 Validation Rules

### Input Validation Hierarchy

```
User Input
    ↓
┌─ Frontend Validation (AdminSLAManagement.tsx)
│   ├─ duration_days >= 1
│   ├─ grace_days >= 0
│   ├─ allow_extensions && max_extensions >= 0
│   ├─ allow_extensions && extension_days >= 0
│   └─ if invalid → show error, prevent save
    │
    ├─ HTTP Request
    │   └─ supabase.from('workflow_sla_policies').update(...)
    │
    └─ Backend Validation (Database)
        ├─ RLS: is user admin?
        │   └─ if not admin → 403 Forbidden
        ├─ Schema: CHECK constraints
        │   ├─ duration_days > 0
        │   ├─ grace_days >= 0
        │   ├─ max_extensions >= 0
        │   └─ if invalid → Database error
        └─ Success: updated_at timestamp
            └─ Return to frontend
                ↓
                Show success message
                (auto-dismiss after 3s)
```

---

**Status:** ✅ COMPLETE  
**Date:** February 25, 2026  
**Ready for Production:** YES

# Supabase Edge Function Environment Variables
# 
# Set these in Supabase Dashboard:
# 1. Go to: https://app.supabase.com → Project → Edge Functions
# 2. Click: generate-full-record-documentation-pdf
# 3. Click: Settings (gear icon) → Environment Variables
# 4. Add the following variables:

# The Node.js PDF server URL
# This is REQUIRED for the Edge Function to proxy PDF requests
# Format: https://your-pdf-server.example.com (no trailing slash)
NODE_PDF_SERVER_URL=https://pdf-server.example.com

# Optional: Debug mode (set to "true" to enable verbose logging)
# DEBUG=false
